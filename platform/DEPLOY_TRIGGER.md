# GWAP Platform Preview Trigger

Triggered after configuring the GitHub Actions secret `CLOUDFLARE_API_TOKEN`.

This file exists only to create a branch push that runs the isolated Cloudflare Workers preview workflow. Production `main` remains unchanged.
