{
  "name": "gwap-landing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/rebuild-blog-images.mjs && eleventy",
    "serve": "node scripts/rebuild-blog-images.mjs && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6"
  }
}
