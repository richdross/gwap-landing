{
  "name": "gwapgang-core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check:worker": "node --check src/worker.js",
    "check:wrangler": "wrangler deploy --dry-run",
    "verify": "npm run check:worker && npm run check:wrangler"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
